# sandbox/

Playground del agente `sandbox-runner`. Todo lo que el agente ejecuta vive
aquí; los hooks capturan cada comando en `logs/events.jsonl`.

- `scripts/fib.py` — script de ejemplo que el agente debe ejecutar.
- `out/` — artefactos generados por el agente en runtime (gitignored).
